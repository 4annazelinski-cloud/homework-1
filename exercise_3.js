const width = 25;
const height = 75;
const length = 50

console.log(`Area: ${width * height} cm`);
console.log(`Perimeter: ${2 * (width + height)} cm`);
console.log(`Volume: ${width * height * length} cm`);