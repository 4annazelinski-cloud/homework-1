Git is a Version Control System, that a tool used to track and manage changes to project's code. Repo is the project folder, a commit is a saved snapshot of project's code, git add moves modified files to a temporary staging area. git push uploads local commits from computer up to the remote repo. git pull downloads theremote repo down to computer. Branch is a parallel timeline which lets copy and modify code without changing the main project.